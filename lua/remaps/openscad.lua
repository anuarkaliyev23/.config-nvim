-- remaps for openscad
vim.g.openscad_default_mappings = false

-- there seems to be no commad `openscad` present upon installation
-- I don't know if there was an API change 
-- It could work if an alias is configured but I wouldn't bother with it
vim.g.openscad_cheatsheet_toggle_key = '<Enter>'

